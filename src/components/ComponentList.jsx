import styled from "styled-components";

const FlexDiv = styled.div`
  background-color: #ddd;

  flex: 1;

  height: 100vh;
`;

const ComponentList = (props) => {
  return <FlexDiv></FlexDiv>;
};

export default ComponentList;
